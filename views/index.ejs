<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <header
      class="bg-white text-black h-7 m-3 text-2xl flex align-middle justify-between"
    >
      <p class="font-bold">Task Manager</p>
      <a
        href="https://www.linkedin.com/in/vaibhavrajharsh"
        class="text-xl text-slate-600"
        >Developer</a
      >
    </header>
    <div class="main w-full min-h-screen bg-zinc-900 p-10 text-white">
      <div class="form p-10">
        <form action="/create" method="post">
          <input
            class="block px-5 py-2 w-full rounded-lg outline-none bg-zinc-800"
            type="text"
            placeholder="Title goes here..."
            name="title"
            maxlength="25"
          />
          <textarea
            class="block w-full rounded-lg mt-2 px-5 outline-none bg-zinc-800 resize-none"
            id="desc"
            placeholder="Write your task details here..."
            name="details"
          ></textarea>
          <input
            type="submit"
            value="Create Task"
            class="mt-2 bg-blue-600 px-5 py-2 rounded-md"
          />
        </form>
      </div>
      <div class="tasks p-10 flex gap-3 flex-wrap">
        <% if(files.length>0){ %> <% files.forEach(function(val){ %>
        <div class="task w-72 px-3 py-4 rounded-md bg-zinc-800 overflow-clip">
          <h1 class="text-white text-2xl tracking-tighter"><%=val%></h1>
          <a href="/edit/<%= val %>" class="text-zinc-400 text-sm">edit name</a>

          <div class="flex justify-between mt-2 w-full">
            <form
              action="/delete/<%= val %>"
              method="post"
              style="display: inline"
            >
              <button
                type="submit"
                class="text-red-500 opacity-70 text-xs mt-3"
              >
                Delete
              </button>
            </form>
            <a href="/file/<%= val %>" class="text-blue-400">Read more</a>
          </div>
        </div>

        <%})%> <% } else { %>
        <h3 class="text-zinc-600">No tasks yet</h3>
        <% } %>
      </div>
    </div>
    <script src="/javascripts/script.js"></script>
  </body>
</html>
